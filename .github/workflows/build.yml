name: Build PixelHeart
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git make libxml2-dev build-essential

      - name: Install ldid
        run: |
          git clone https://github.com/ProcursusTeam/ldid.git ~/ldid
          cd ~/ldid
          ./make.sh
          sudo cp ldid /usr/local/bin/

      - name: Install Theos
        run: |
          git clone --recursive https://github.com/theos/theos.git ~/theos
          echo "THEOS=$HOME/theos" >> $GITHUB_ENV

      - name: Download SDK
        run: |
          cd $HOME/theos
          git clone https://github.com/theos/sdks.git sdks

      - name: Build and package
        run: |
          make clean
          make package

      - name: Upload DEB
        uses: actions/upload-artifact@v4
        with:
          name: PixelHeart-deb
          path: packages/*.deb
